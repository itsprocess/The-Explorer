export const discoveryQuery=`SELECT v.character,json_extract(c.value,'$.name') AS name FROM visits v LEFT JOIN characters c ON c.id=v.character WHERE v.x=? AND v.y=? ORDER BY v.at,v.id LIMIT 1`;
export const imprintQuery=`SELECT json_extract(value,'$.event.imprint') AS imprint FROM visits WHERE x=? AND y=? AND json_extract(value,'$.event.kind') IN ('interaction','acquisition','honor','death','teleport') ORDER BY at,id LIMIT 1`;
