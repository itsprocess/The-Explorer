Adjacent ocean, void, chasm, lake and river fields are passed directionally into setting interpretation and image context. Neighboring blockers are visible geography and shape the occupied biome; the AI authors the resulting coastal or edge setting without a prescribed label list.

Inaccessible warp destinations get a saved AI-authored physical scene and fatal arrival, plus the same one-time asynchronous image schedule as other locations. Normal fatal teleports place the dead character at that destination. Saved images are reused.

Dev mode remains active through compass travel, options, teleports, death and return to origin. Compass travel may preview blocked destinations and continue after simulated death. Only Return to normal exits the preview; real discovery, badges and run state remain untouched.

The 0022 migration wipes world/character progress and images once, retaining account identities, permanent defining traits and sessions.
