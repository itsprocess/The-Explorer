# Thresholds update

Seed: `the-explorer-thresholds-20260907`. The reset clears cells, images, visits, claims, badges, deaths and distance, returning existing characters to origin. Names, password hashes and sessions survive.

One opaque loading panel replaces the map and location until the movement response and actual image load both finish. Concealed content is inert and hidden from assistive technology. Encounter notifications and sound are deferred until reveal. Failed image delivery offers retry without regenerating an existing illustration.

Image generation uses an additional art-only threat input after the canonical text is saved. Lethal cell rules supply the threat even when neighbors are preloaded before discovery. If a death has already been committed at this coordinate when the first illustration is generated, its historical record can also influence a discreet, non-gory remnant. Conditional hostility only supplies this context after a confirmed death. Safe approaches never receive death imagery. Text, arrival rules and discovery status are unchanged. Once saved, illustrations remain permanent: later deaths do not regenerate them.

The sidebar and location sections use restrained cards. “Share this location” continues to open a read-only location page; it never moves a character.
