# Challenge achievements and visible participants

A failed requirement check cannot grant an achievement badge. Normalize generated failure badge outcomes to none and enforce the same rule when resolving older cached encounters. Failed-branch honor badges from the previous bug are excluded from badge lists; death records remain valid. Passing branches retain their assigned results.

The reported current-seed tile (1,1) had zero population and a Strength challenge with badges assigned to both branches. A focused deterministic check reproduces the assignment error. The screenshot scene itself introduced a generic character; image generation consumed that scene. No player identity is supplied in the image payload. Exact provenance inside the cached encounter setup was not available in the bounded database projection.

New setup, scene and image guidance treats the player as an unseen viewpoint. Residents and specific encounter counterparts remain permitted. No saved text/image regeneration, wipe, seed change or paid-generation test.
