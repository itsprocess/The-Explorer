Affiliations persist through death: one -100 to +100 standing per family and underlying enum value, starting at zero. Known shared names group under their scale. Encounter requirements compare standing to an explicit minimum. Assigned outcome shifts raise or lower it, clamped to bounds; each outcome shifts standing at most once per life to prevent repeat-click farming. Dev previews never retain changes in the real run.
Devoutness is independent smooth civilization noise, masked by the combined footprint. It increases the likelihood of affiliation checks and shifts among the local faction, kingdom and religion pools without dictating friendliness. Affiliation rewards use scales rather than replaceable inventory/status tokens.
Peeks expose enclosure and the shared threshold, without secrets. Origin remains protected and uses AI-authored sanctuary imagery, not a supplied landmark or active encounter.
Explicit wipe default: delete accounts and all world data, change seed; no in-app account-deletion control.
