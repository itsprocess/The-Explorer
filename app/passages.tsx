export default function Passages({exits,blocked=[],onMove,onIntent,disabled=false}:{exits:{direction:string;description:string;glimpse?:string}[];blocked?:{direction:string;reason:string}[];onMove?:(direction:string)=>void;disabled?:boolean;onIntent?:(direction:string)=>void}){
 return <div className="passages-card"><ul className="passage-list">{exits.map(e=><li key={e.direction}><strong>{e.direction}</strong><div><p>{e.description}</p></div>{onMove&&<button className="passage-move" disabled={disabled} onMouseEnter={()=>onIntent?.(e.direction)} onFocus={()=>onIntent?.(e.direction)} onClick={()=>onMove(e.direction)}>Go {e.direction[0].toUpperCase()+e.direction.slice(1)}</button>}</li>)}{blocked.map(b=><li className="closed-passage" key={b.direction}><strong>{b.direction}</strong><div><p>{b.reason}</p></div></li>)}</ul></div>;
}
