import baseline from '../lib/fieldwork-baseline.json';
import {affiliationFamilies,type Standing} from '../lib/affiliations';
export default function AffiliationDisplay({standings=[]}:{standings?:Standing[]}){
 return <section className="affiliations"><h2>Affiliations</h2><p className="muted">Standings survive death. Shared names follow the same underlying affiliation.</p>{affiliationFamilies.map(f=><details key={f} open={standings.some(s=>s.family===f)}><summary>{f[0].toUpperCase()+f.slice(1)}</summary>{baseline.variables.find(v=>v.appName==='civilization.'+f)!.steps.map((step,i)=>{const s=standings.find(s=>s.family===f&&s.value===step.id),score=s?.score??0;return <article className="standing" key={step.id}><div><strong>{s?.names.length?s.names.join(' · '):'Not yet encountered'}</strong><span>{score>0?'+':''}{score}</span></div><meter min={-100} max={100} value={score} aria-label={(s?.names.join(', ')||'Not yet encountered')+' standing'}/><div className="scale-ends"><span>Disfavored</span><span>Neutral</span><span>Favored</span></div></article>;})}</details>)}</section>;
}
